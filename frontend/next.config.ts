import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['10.1.52.220','new-delivery.rithyboth.work', 'localhost'],
};

export default nextConfig;
